name: pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build_docker_image:
    needs: []
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build Docker image
      run: docker build -t lukasdext01/minha-api:latest .

    - name: Log in to Docker Hub (if needed)
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Push Docker image to Docker Hub (if needed)
      run: docker push lukasdext01/minha-api:latest

  deploy_to_kubernetes:
    needs: [build_docker_image]
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install Google Cloud SDK
      run: 
        curl https://sdk.cloud.google.com | bash

    - name: Authenticate to Google Cloud
      id: auth
      uses: 'google-github-actions/auth@v1'
      with:
        credentials_json: '${{ secrets.GOOGLE_CREDENTIALS }}'

    - name: Instalando pacotes do google
      run: |
        sudo rm /etc/apt/sources.list.d/google-cloud-sdk.list
        echo "deb [signed-by=/usr/share/keyrings/cloud.google.gpg] https://packages.cloud.google.com/apt cloud-sdk main" | sudo tee -a /etc/apt/sources.list.d/google-cloud-sdk.list
        sudo apt-get update
      
        
    - name: Install gke-gcloud-auth-plugin
      run: |
        sudo apt-get update
        sudo apt-get install google-cloud-sdk-gke-gcloud-auth-plugin

    - name: Configure Kubernetes context
      run: |
        gcloud container clusters get-credentials my-cluster --zone us-central1-a --project teste-cluster-392922

    - name: Deploy to Kubernetes 
      run: |
        kubectl apply -f deployment.yml
        kubectl apply -f service.yml

#sudo apt-get install google-cloud-sdk-gke-gcloud-auth-plugin
#sudo apt-get update && sudo apt-get --only-upgrade install google-cloud-sdk-datastore-emulator google-cloud-sdk-kubectl-oidc kubectl google-cloud-sdk-nomos google-cloud-sdk-log-streaming google-cloud-sdk-harbourbridge google-cloud-sdk-enterprise-certificate-proxy google-cloud-sdk-spanner-emulator google-cloud-sdk-skaffold google-cloud-sdk-kpt google-cloud-sdk-local-extract google-cloud-sdk-app-engine-java google-cloud-sdk-minikube google-cloud-sdk-config-connector google-cloud-sdk-firestore-emulator google-cloud-sdk-app-engine-go google-cloud-sdk-cbt google-cloud-sdk google-cloud-sdk-package-go-module google-cloud-sdk-gke-gcloud-auth-plugin google-cloud-sdk-app-engine-python-extras google-cloud-sdk-app-engine-python google-cloud-sdk-terraform-tools google-cloud-sdk-pubsub-emulator google-cloud-sdk-app-engine-grpc google-cloud-sdk-cloud-build-local google-cloud-sdk-cloud-run-proxy google-cloud-sdk-bigtable-emulator google-cloud-sdk-anthos-auth